{
  "name": "clipmer",
  "version": "3.1.0",
  "description": "Secrets-aware clipboard manager for Linux",
  "author": {
    "name": "Mustafa",
    "email": "mustafa@clipmer.local"
  },
  "homepage": "https://github.com/0x99M/clipmer",
  "main": "main.js",
  "scripts": {
    "start": "electron . --no-sandbox",
    "build": "electron-builder --linux AppImage deb rpm"
  },
  "dependencies": {
    "electron-store": "8.2.0"
  },
  "devDependencies": {
    "electron": "^35.0.0",
    "electron-builder": "^26.0.0"
  },
  "build": {
    "appId": "com.clipmer.app",
    "productName": "Clipmer",
    "linux": {
      "target": ["AppImage", "deb", "rpm"],
      "icon": "assets/icons/",
      "category": "Utility"
    },
    "deb": {
      "depends": ["at-spi2-core"],
      "afterInstall": "build/postinst.sh",
      "afterRemove": "build/afterRemove.sh"
    },
    "rpm": {
      "depends": ["at-spi2-core"],
      "afterInstall": "build/postinst.sh",
      "afterRemove": "build/afterRemove.sh"
    }
  }
}
